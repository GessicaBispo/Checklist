import { Text, View } from 'react-native';

export default function CreateOccurrence() {
    return (
        <View style={{ flex: 1, padding: 16, justifyContent: 'center' }}>
            <Text>Create Occurrence</Text>
        </View>
    );
}
